import React from 'react';
import Header from './Header.js';

export default class Playlist extends React.Component {
	render() {
		// as long as first line has a tag no need for parenthesis
		return <div>
			<Header />
			Not done 
		</div>;
	};
};
